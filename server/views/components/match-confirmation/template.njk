{% if params.matchName %}
    {% set matchName = params.matchName | properCase | removeTitle %}
    {% if params.matchType === 'No record' %}
        {%- set messageText = 'You have successfully confirmed ' + matchName + ' has no NDelius record.' -%}
    {% elif params.matchType === 'linked' %}
        {%- set messageText = 'You have successfully linked an NDelius record to ' + matchName + '.' -%}
    {% elif params.matchType === 'unlinked' %}
        {%- set messageText = 'You have successfully unlinked an NDelius record from ' + matchName + '.' -%}
    {% else %}
        {%- set messageText = 'You have successfully confirmed a record for ' + matchName + '.' -%}
    {% endif %}

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            {%- from "moj/components/banner/macro.njk" import mojBanner -%}
            {{ mojBanner({
                type: 'success',
                text: messageText,
                iconFallbackText: 'Success'
            }) }}
        </div>
    </div>
{% endif %}
